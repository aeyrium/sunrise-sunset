name: sunrise_sunset
description: Dart API that provides sunset and sunrise times for a given latitude and longitude
version: 1.0.4

homepage: https://aeyrium.com/products/mxtracker/
repository: https://github.com/aeyrium/sunrise-sunset

environment:
  sdk: ">=3.0.0-226.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5

dev_dependencies:
  test: any
  flutter_test:
    sdk: flutter
